#include <stdio.h>
#include <string.h>

/*
 * convert takes in char * with word to be converted
 * and returns char * to word converted to pig_latin
 */
char *convert(char *word) {
	int pig_word_len = /* fill in length */;
	char *pig_word = /* allocate space based on pig_word_len */;

	/* copy all letters EXCLUDING the first letter to new word */
	

	/* copy first letter to end of new word */


	/* append "ay" to end of new word */

	return pig_word;	
}
